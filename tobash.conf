# >>> BASHRC PROMPT INIT >>>
if [ -f ~/.config/gr8sh/prompt.sh ]; then
        source ~/.config/gr8sh/prompt.sh
fi
# <<< BASHRC PROMPT INIT <<<

alias cs='clear'
alias lsa='ls -a'
alias update='sudo nala update && echo "Updating Flatpaks" && flatpak update'
alias upgrade='sudo nala upgrade -y'
alias upgradable='nala list --upgradable'
alias m=micro
alias msc='cd ~/Projects/msresults'
alias df='cd ~/Projects/dotfiles'
alias projs='cd ~/Projects'
alias temps='curl wttr.in/Campinas'

curr_temp="$(curl -s wttr.in/Campinas?format=3)"

export PATH="/home/thzgr/.local/bin:$PATH"

